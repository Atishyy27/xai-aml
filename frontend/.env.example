# Base URL of the SENTINEL API. Required for any deployed build.
# Local dev leaves this unset and falls back to http://127.0.0.1:8000.
#
# Vercel: set this in Project -> Settings -> Environment Variables, not here.
VITE_API_URL=https://<your-user>-<your-space>.hf.space
